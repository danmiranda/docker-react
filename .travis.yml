sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t danimiranda/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true danimiranda/docker-react npm run test
